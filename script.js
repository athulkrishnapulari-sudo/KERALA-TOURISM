const btn=document.getElementById("nav_btn");
const box=document.querySelector(".links");
const icon=document.getElementById("ham_icon")

btn.addEventListener('click',()=>{
    if (links.classList.contains('show')){
        links.classList.remove('show');
        icon.style.transform='rotate(0deg)';
        
    }else{
        links.classList.add('show');
        icon.style.transform='rotate(90deg)';
    }
});

const item5=document.getElementById('nav_item5');



const item1=document.getElementById('nav_item1');

item1.addEventListener('click',()=>{
    if (links.classList.contains('show')){
        links.classList.remove('show');
        icon.style.transform='rotate(0deg)';
        
    }else{
        links.classList.add('show');
        icon.style.transform='rotate(90deg)';
    }
})

const item2=document.getElementById('nav_item2');

item2.addEventListener('click',()=>{
    if (links.classList.contains('show')){
        links.classList.remove('show');
        icon.style.transform='rotate(0deg)';
        
    }else{
        links.classList.add('show');
        icon.style.transform='rotate(90deg)';
    }
})

const item3=document.getElementById('nav_item3');

item3.addEventListener('click',()=>{
    if (links.classList.contains('show')){
        links.classList.remove('show');
        icon.style.transform='rotate(0deg)';
        
    }else{
        links.classList.add('show');
        icon.style.transform='rotate(90deg)';
    }
})
item5.addEventListener('click',()=>{
    if (links.classList.contains('show')){
        links.classList.remove('show');
        icon.style.transform='rotate(0deg)';
        
    }else{
        links.classList.add('show');
        icon.style.transform='rotate(90deg)';
    }
})


const item4=document.getElementById('nav_item4');

item4.addEventListener('click',()=>{
    if (links.classList.contains('show')){
        links.classList.remove('show');
        icon.style.transform='rotate(0deg)';
        
    }else{
        links.classList.add('show');
        icon.style.transform='rotate(90deg)';
    }
})